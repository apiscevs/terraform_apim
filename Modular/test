<style>
  /* Optional: add a smooth transition for the height change */
  details {
    overflow: hidden;
    transition: max-height 0.3s ease;
  }
  /* When collapsed, limit the max-height to just the summary */
  details:not([open]) {
    max-height: 40px; /* adjust to the approximate height of your summary */
  }
  /* When expanded, allow it to grow */
  details[open] {
    max-height: 1000px; /* a large value so it can expand fully */
  }
</style>

<details>
  <summary style="cursor: pointer; font-size: 1.2em; font-weight: bold;">
    Click to Expand
  </summary>
  <div>
    <p>This is the detailed content. When you click the summary, this section will expand and push the sections below it downward.</p>
    <p>You can add as much content as you need here.</p>
  </div>
</details>
